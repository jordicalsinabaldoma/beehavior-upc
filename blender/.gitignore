# The scene is rebuilt from the .py files: the .blend is output, not source.
# Packed with the HDRI and textures it weighs ~71 MB, not worth versioning.
*.blend
*.blend[0-9]

# Assets downloaded from Poly Haven (CC0). The scripts fetch them on demand.
assets/

# Renders. Downscaled copies of the final six live in ../docs/renders/.
renders/
